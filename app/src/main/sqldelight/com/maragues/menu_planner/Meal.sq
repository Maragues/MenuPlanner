import org.threeten.bp.ZonedDateTime;

CREATE TABLE IF NOT EXISTS meal (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    happensAt INTEGER AS ZonedDateTime NOT NULL,
    name TEXT NOT NULL
);

select_all:
SELECT *
FROM meal;